# LS-PIE
Contains the LS-PIE module and example notebooks showcasing the application in Python.
The main_program notebook utilizes the contained methods on a simple sinusoidal problem showcasing their use as both free standing classes as well as combined into a single "processor" object.
